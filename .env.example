# Get this from platform.claude.com (Settings -> API Keys). It's shown in
# full only once, at creation — save it to a password manager right after,
# so a new computer just means paste-into-.env, not "make a new key."
# This .env file itself is git-ignored on purpose: never commit a real key.
ANTHROPIC_API_KEY=
